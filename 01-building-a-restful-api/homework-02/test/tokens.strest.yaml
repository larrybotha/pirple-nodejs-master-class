version: 2

variables:
  endpoint: /tokens

requests:
  tokens_create:
    log: true
    request:
      url: <$ globalBaseUrl $><$ endpoint $>
      method: POST
    validate:
    - jsonpath: content.id
      type: string
